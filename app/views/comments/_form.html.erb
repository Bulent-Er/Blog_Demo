<%= form_with(model: [post, comment]) do |form| %>
  <div class="field">
    <%= form.rich_text_area :body, placeholder: "Your comment here" %>
  </div>
  <div class="actions">
    <%= form.submit submit_label, class: "btn btn-success mt-1" %>
  </div>
<% end %>