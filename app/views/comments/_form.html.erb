<h2>Add a Comment</h2>
<%= form_with(model: [post, post.comments.build ]) do |form| %>
  <div class="field">
    <%= form.rich_text_area :body, placeholder: "Your comment here" %>
  </div>
  <div class="actions">
    <%= form.submit "Reply", class: "btn btn-success mt-1" %>
  </div>
<% end %>