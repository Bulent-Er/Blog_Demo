<h1 class= "text-center"> <%= pluralize(@posts.count, "Result")%> </h1>
<div class="container">
  <table class="table table-dark table-striped">
    <thead>
      <tr>
        <th scope="col"> <%= sort_link(@query, :title, "Title", default_order: :asc) %></th>
        <th scope="col"> <%= sort_link(@query, :body, "Blog Content")%></th>
        <th scope="col"> <%= sort_link(@query, :user_name, "Created By") %></th>
        <th scope="col"> <%= sort_link(@query, :created_at) %></th>
        <th scope="col"> <%= sort_link(@query, :updated_at) %></th>
      </tr>
    </thead>
    <body>
      <% @posts.each do |post| %>
        <tr>
          <td> <%= link_to post.title.truncate_words(5), post_path(post) %> </td>
          <td> <%= post.body.to_plain_text.truncate_words(10) %> </td>
          <td> <%= link_to post.user.name, user_path(post.user) %> </td>
          <td> <%= post.created_at.strftime("%Y-%b-%d %H:%M:%S") %> </td>
          <td> <%= post.updated_at.strftime("%Y-%m-%d") %> </td>
        </tr>
      <% end %>
    </body>
  </table>

</div>